{
  "name": "kitty-remote-deck",
  "version": "0.2.0",
  "private": true,
  "description": "A local web front-end for browsing and controlling local or SSH kitty GUI sessions.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "smoke:chrome": "node scripts/chrome-smoke.mjs"
  }
}
